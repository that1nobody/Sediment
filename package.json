{
  "name": "sediment",
  "version": "0.1.0",
  "description": "Procedural world-history generator",
  "scripts": {
    "test": "jest --config jest.config.js",
    "test:coverage": "jest --config jest.config.js --coverage",
    "test:watch": "jest --config jest.config.js --watch",
    "generate": "npx tsc && node dist/run.js",
    "chronicle": "npm run generate && python render_chronicle.py"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.4.5"
  }
}
